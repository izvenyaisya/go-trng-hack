//go:build ignore
// +build ignore

// This file is intentionally ignored by the build. It used to be a cgo
// wrapper that duplicated `RunMonobit` with `nist.go`. Keep the file as
// documentation/history but exclude it from compilation.

package nist

// Note: build tag `ignore` above prevents this file from being compiled.

